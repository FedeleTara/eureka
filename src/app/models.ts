export type Quote = {
    author: string;
    sentence: string;
    date_creation: string;
    id: string;
}

export type QuoteGenerated = {
    author: string;
    category: string;
    quote: string;
};